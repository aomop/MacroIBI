% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_autosave.R
\name{build_autosave_df}
\alias{build_autosave_df}
\title{Flatten selected genera into an autosave-friendly data.frame}
\usage{
build_autosave_df(selected_snapshot, group_defs)
}
\arguments{
\item{selected_snapshot}{A named list. Each element name is a section_id
(e.g. "section_1"), and each element is a list with a \code{data} element.
The \code{data} element should itself be a list of rows, where each row is a
list containing at least: taxon, dipnet1, dipnet2, tsn, parentTsn.}

\item{group_defs}{A data.frame with columns:
\itemize{
\item section_id
\item group_id
\item group_name
}}
}
\value{
A data.frame with one row per taxon to be auto-saved, or NULL if
there is no data to save.
}
\description{
This mirrors the logic used inside autosave_module_server(), but works on
a plain snapshot of \code{selected_genera} instead of live reactives. It is
designed for unit testing and for use inside the server module.
}
\keyword{internal}
