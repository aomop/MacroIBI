linters: lintr::linters_with_defaults(
  line_length_linter = lintr::line_length_linter(120),
  object_usage_linter = NULL   # disable “no visible …” style complaints
)
exclusions: list(
  "auto_saves",
  "metric_autosaves",
  ".github",
  "renv"
)
